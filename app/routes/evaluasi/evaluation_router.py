"""Placeholder evaluation router moved under app.routes.evaluasi.

If you have evaluation endpoints, add them here. Keeping a small
placeholder prevents import errors when other modules import the router.
"""

from fastapi import APIRouter

router = APIRouter()

# Add evaluation endpoints here when ready
